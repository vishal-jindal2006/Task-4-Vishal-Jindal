services:
  - type: web
    name: ai-vision-recognition-system
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y tesseract-ocr
    startCommand: gunicorn app:app